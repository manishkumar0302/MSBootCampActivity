spring:
  cloud:
    config:
      uri: ${currencyms.configserverurl:http://localhost:8888}
      fail-fast: false
  application:
    name: currencyms
  profiles:    active: dev

server:  port: 8082