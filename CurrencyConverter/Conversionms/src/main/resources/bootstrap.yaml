spring:
  cloud:
    config:
      uri: ${conversionms.configserverurl:http://kubernetes.docker.internal:8888}
      fail-fast: false
  application:
    name: conversionms
  profiles:    active: dev

